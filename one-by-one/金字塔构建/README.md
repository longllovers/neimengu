# TIF 金字塔构建台

本地 Web 工具，按文件使用独立 Python 子进程，分两个阶段处理 TIF：

1. 为全部 TIF 生成外部 `.ovr`；
2. 外部阶段全部结束后，为全部 TIF 写入内部金字塔。

页面可设置最高金字塔倍数（最高及默认值为 256）、并发子进程数、重采样方式、递归扫描、强制重建和仅检查模式。运行输出只显示文件进度与结果。

## 启动

```powershell
uv run python app.py
```

浏览器打开 <http://127.0.0.1:8899>。
